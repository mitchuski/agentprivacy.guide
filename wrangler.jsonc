// Workers deploy config for guide.agentprivacy.ai (project: agentprivacy-guide).
// The site is PREBUILT locally (snapshot → gate → verify); the cloud only uploads.
// Local direct deploy: `npx wrangler deploy` (no flags needed with this file).
{
  "name": "agentprivacy-guide",
  "compatibility_date": "2026-07-01",
  "assets": {
    "directory": "./site"
  }
}
